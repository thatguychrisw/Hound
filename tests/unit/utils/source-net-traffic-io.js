import {module, test} from 'qunit';

// The idea of this utility, is to use a vendor specific source to create instances
// of the net-traffic-io model.
module('Unit | Utility | get network traffic input/output');

test('test loaded', function (assert) {
    // Not all that useful, just for example's sake
    assert.ok(true);
});